# Voinox AI Corp

```bash
npm install
npm run dev
```

## Calendly

Copy `.env.example` to `.env` and set **VITE_CALENDLY_URL** to your Calendly scheduling page URL (e.g. `https://calendly.com/your-username/30min`). The Contact page shows the inline scheduler.
